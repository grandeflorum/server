<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.grandeflorum.StockHouse.dao.StockHouseMapper">
    <resultMap id="BaseResultMap" type="com.grandeflorum.StockHouse.domin.StockHouse">
        <!--
          WARNING - @mbg.generated
        -->
        <id column="ID" jdbcType="VARCHAR" property="id"/>
        <result column="CQRXM" jdbcType="VARCHAR" property="cqrxm"/>
        <result column="ADDRESS" jdbcType="VARCHAR" property="address"/>
        <result column="AUDIT_TYPE" jdbcType="VARCHAR" property="auditType"/>
    </resultMap>

    <!--获取存量房源查询列表-->
    <select id="getStockHouseList" resultMap="BaseResultMap">
        select * from StockHouse p where 1=1
        <if test="cqrxm!=null and cqrxm != ''">
            and (s.cqrxm like '%'||#{cqrxm}||'%' )
        </if>
        <if test="address!=null and address != ''">
            and (s.address like '%'||#{address}||'%' )
        </if>
        <if test="auditType!=null and auditType != ''">
            and s.audit_type=#{auditType}
        </if>
        order by p.SYS_DATE desc
    </select>

    <!--添加或更新审核-->
    <insert id="addOrUpdateAudit" parameterType="com.grandeflorum.StockHouse.domin.StockHouse">
        <selectKey keyProperty="count" resultType="int" order="BEFORE">
            select count(*) from wf_audit p where p.PROJECTID=#{projectId,jdbcType=VARCHAR}
        </selectKey>
        <if test="count>0">
            update wf_audit p set
            p.shsj=#{shsj,jdbcType=TIMESTAMP},
            p.shr=#{shr,jdbcType=VARCHAR},
            p.shrq=#{shrq,jdbcType=TIMESTAMP},
            p.bz=#{bz,jdbcType=VARCHAR},
            p.SYS_UPD_DATE=#{sysdate,jdbcType=TIMESTAMP}
            where p.id = #{projectId,jdbcType=VARCHAR}
        </if>
        <if test="count==0">
            insert into wf_audit(
            id,
            shsj,
            shr,
            shrq,
            bz,
            PROJECTID,
            SYS_DATE
            )
            values (
            #{id,jdbcType=VARCHAR},
            #{shsj,jdbcType=TIMESTAMP},
            #{shr,jdbcType=VARCHAR},
            #{shrq,jdbcType=TIMESTAMP},
            #{bz,jdbcType=VARCHAR},
            #{projectId,jdbcType=VARCHAR},
            #{sysdate,jdbcType=TIMESTAMP}
            )
        </if>
    </insert>

</mapper>